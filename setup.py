from setuptools import setup,find_packages


setup(
    name="hatespeechproject",
      version='0.0.1',
      long_description="this is the nlp project to classify the hate speeches",
      author='Ankit Mahalle',
      packages=find_packages(),
      install_requires=[]
      )
